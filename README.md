# Ngamia-Marquee-CUstomer-Portal-V2

Funlings Entertainment Portal

Prerequisites

Nodejs

To RUN the project in your local environment:

1. Clone the repo
2. Run `yarn install`
3. Run `yarn start`

`WeHaveBuiltThisProjectWithPassionThusGuardItWell`
